//: Playground - noun: a place where people can play

import UIKit

/**
 *  集合的可变性：
        -> 如果创建一个Arrays、Sets或Dictionaries并且把它分配一个变量，这个集合将会是可变的。
        -> 如果创建一个Arrays、Sets或Dictionaries并且把它分配一个常量，这个集合将会是不可变的。
 */
